{
  "name": "@tabnas/support",
  "version": "0.3.2",
  "main": "dist/support.js",
  "type": "commonjs",
  "types": "dist/support.d.ts",
  "description": "Shared test-support utilities for the tabnas parser system: the TSV spec-fixture loader, escape codec, expectation helpers and cross-runtime test runner.",
  "homepage": "https://github.com/tabnas/support",
  "keywords": [
    "tabnas",
    "parser",
    "test",
    "fixture",
    "tsv",
    "conformance"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --build src",
    "watch": "tsc --build src -w",
    "clean": "rm -rf node_modules dist package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "test": "npm run build && node --enable-source-maps --test test/*.test.js",
    "coverage": "npm run build && node --test --experimental-test-coverage --test-coverage-include=dist/*.js --test-coverage-lines=0.9 test/*.test.js",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: ts/v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag ts/v$REPO_VERSION && git push origin ts/v$REPO_VERSION;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag"
  },
  "files": [
    "LICENSE",
    "dist"
  ],
  "exports": {
    ".": "./dist/support.js",
    "./adder": "./dist/adder.js",
    "./escape": "./dist/escape.js",
    "./expect": "./dist/expect.js",
    "./spec": "./dist/spec.js"
  },
  "devDependencies": {
    "@tabnas/parser": "*",
    "@types/node": "22.x",
    "typescript": "5.x"
  },
  "peerDependencies": {
    "@tabnas/parser": ">=0"
  },
  "peerDependenciesMeta": {
    "@tabnas/parser": {
      "optional": true
    }
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/support.git",
    "directory": "ts"
  }
}
